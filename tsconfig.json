{
  "//": "Root tsconfig exists only to aid editors (solution-style references). Builds and typechecks use each package's own tsconfig (extending @monorepo-boilerplate/ts-config). It is deliberately not in turbo.json globalDependencies, so editing references never busts the cache. When scaffolding a package, add it to references below.",
  "files": [],
  "references": [
    { "path": "apps/web" },
    { "path": "mcp-servers/example" },
    { "path": "mcp-servers/ui" },
    { "path": "packages/types" },
    { "path": "packages/logger" },
    { "path": "packages/ui" },
    { "path": "packages/environment" },
    { "path": "packages/providers" },
    { "path": "packages/database" },
    { "path": "packages/auth" },
    { "path": "tooling/catalog-lint" },
    { "path": "tooling/catalog-extractor" },
    { "path": "tooling/annotations" },
    { "path": "docs" }
  ]
}
