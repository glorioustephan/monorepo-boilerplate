{
  "name": "@monorepo-boilerplate/catalog-lint",
  "version": "0.0.0",
  "private": true,
  "description": "Custom checks oxlint can't express: token-bypass + catalog AST rules (raw-HTML/ui-import).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@monorepo-boilerplate/ui": "workspace:*",
    "ts-morph": "catalog:"
  },
  "devDependencies": {
    "@monorepo-boilerplate/test-config": "workspace:*",
    "@monorepo-boilerplate/ts-config": "workspace:*",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
