{
  "name": "@monorepo-boilerplate/test-config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Vitest and Playwright presets consumed from source.",
  "type": "module",
  "exports": {
    "./vitest": "./src/vitest.ts",
    "./playwright": "./src/playwright.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vitejs/plugin-react": "catalog:",
    "jsdom": "catalog:"
  },
  "devDependencies": {
    "@monorepo-boilerplate/ts-config": "workspace:*",
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "@playwright/test": "catalog:",
    "vitest": "catalog:"
  }
}
