/**
 * Database layer — placeholder.
 *
 * This package will own schema definitions, query helpers, and migrations once
 * an ORM is chosen (see roadmap Phase 2). It is intentionally ORM-agnostic for
 * now so the boilerplate doesn't impose a database decision on consumers.
 */
export const DATABASE_NOT_CONFIGURED = true as const;
