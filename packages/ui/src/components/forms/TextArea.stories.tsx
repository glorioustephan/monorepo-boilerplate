// Code generated by @monorepo-boilerplate/catalog-codegen. DO NOT EDIT.
// Edit packages/ui/src/components/components.manifest.ts and run `pnpm ui:codegen`.
import { TextArea, Flex } from '@monorepo-boilerplate/ui';
import type { Meta, StoryObj } from '@storybook/react-vite';

const meta = {
  title: 'Forms/TextArea',
  component: TextArea,
  argTypes: {
    variant: { control: { type: 'select' }, options: ['classic', 'surface', 'soft'] },
    size: { control: { type: 'select' }, options: ['1', '2', '3'] },
  },
  render: (args) => <TextArea {...args} />,
  parameters: { layout: 'padded' },
} satisfies Meta<typeof TextArea>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {};

export const Variant: Story = {
  render: () => (
    <Flex gap="3" align="center" wrap="wrap">
      {(['classic', 'surface', 'soft'] as const).map((value) => (
        <TextArea placeholder="Write…" key={value} variant={value} />
      ))}
    </Flex>
  ),
};

export const Size: Story = {
  render: () => (
    <Flex gap="3" align="center" wrap="wrap">
      {(['1', '2', '3'] as const).map((value) => (
        <TextArea placeholder="Write…" key={value} size={value} />
      ))}
    </Flex>
  ),
};
