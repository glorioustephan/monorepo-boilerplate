{
  "name": "@monorepo-boilerplate/docs",
  "version": "0.0.0",
  "private": true,
  "description": "Project documentation — VitePress site. Canonical agent/code conventions stay in AGENTS.md + .claude/reference/.",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@monorepo-boilerplate/ts-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitepress": "catalog:",
    "vue": "catalog:"
  }
}
